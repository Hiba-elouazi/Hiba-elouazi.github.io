# Hiba El Ouazi, portfolio

Static site exported from the design canvas. No build step.

- index.html : the page (template plus a small logic class)
- support.js, vendor/ : the small runtime that renders it in the browser
- assets/ : photo and the two CVs (English and French)

To replace a CV, overwrite the PDF in assets/ keeping the exact same filename.
